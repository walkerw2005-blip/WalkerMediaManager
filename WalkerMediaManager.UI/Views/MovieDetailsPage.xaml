<Page
    x:Class="WalkerMediaManager.UI.Views.MovieDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WalkerMediaManager.UI.Controls"
    xmlns:models="using:WalkerMediaManager.UI.Models">

    <Grid>
        <ScrollViewer>
            <Grid Padding="32" RowSpacing="22">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid ColumnSpacing="14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Content="Back" Click="BackButton_Click" VerticalAlignment="Top" />
                    <StackPanel Grid.Column="1" Spacing="4">
                        <TextBlock x:Name="PageTitleText" Text="Movie Details" Style="{ThemeResource TitleTextBlockStyle}" />
                        <TextBlock Text="Collection record, owned editions, and Plex metadata" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    </StackPanel>
                </Grid>

                <InfoBar x:Name="StatusInfoBar" Grid.Row="1" IsOpen="False" IsClosable="True" />

                <Grid Grid.Row="2" ColumnSpacing="28">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="280" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="14">
                        <Border Height="410" CornerRadius="14"
                                Background="{ThemeResource ControlFillColorSecondaryBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <controls:CachedArtworkImage x:Name="PosterImage" />
                        </Border>
                        <Border Padding="14" CornerRadius="10"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Collection status" FontWeight="SemiBold" />
                                <TextBlock x:Name="OwnershipText" Text="No owned copies recorded" />
                                <TextBlock x:Name="FormatText" Text="Add a copy to track format and edition" Opacity="0.7" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Spacing="18">
                        <StackPanel Spacing="8">
                            <TextBlock x:Name="TitleText" Text="Movie" FontSize="38" FontWeight="SemiBold" TextWrapping="Wrap" />
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <Border Padding="9,4" CornerRadius="6" Background="{ThemeResource ControlFillColorSecondaryBrush}">
                                    <TextBlock x:Name="YearText" Text="Year unknown" />
                                </Border>
                                <Border Padding="9,4" CornerRadius="6" Background="{ThemeResource ControlFillColorSecondaryBrush}">
                                    <TextBlock x:Name="RatingText" Text="Not rated" />
                                </Border>
                                <Border Padding="9,4" CornerRadius="6" Background="{ThemeResource ControlFillColorSecondaryBrush}">
                                    <TextBlock x:Name="RuntimeText" Text="Runtime unknown" />
                                </Border>
                            </StackPanel>
                        </StackPanel>

                        <Border Padding="20" CornerRadius="12"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <StackPanel Spacing="14">
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="3">
                                        <TextBlock Text="Owned copies" FontSize="20" FontWeight="SemiBold" />
                                        <TextBlock x:Name="CopiesSummaryText" Text="Track formats, editions, purchases, and shelf locations." Opacity="0.7" />
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="Add owned copy" Click="AddCopyButton_Click" />
                                </Grid>

                                <TextBlock x:Name="NoCopiesText" Text="No owned copies have been recorded for this movie." Opacity="0.7" />

                                <ListView x:Name="OwnedCopiesList" SelectionMode="None">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:OwnedCopy">
                                            <Border Padding="14" Margin="0,0,0,8" CornerRadius="10"
                                                    Background="{ThemeResource ControlFillColorSecondaryBrush}">
                                                <Grid ColumnSpacing="14">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Spacing="5">
                                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                                            <TextBlock Text="{x:Bind DisplayName}" FontSize="17" FontWeight="SemiBold" />
                                                            <TextBlock Text="{x:Bind FavoriteDisplay}" FontWeight="SemiBold" Opacity="0.7" />
                                                        </StackPanel>
                                                        <TextBlock Text="{x:Bind PackagingDisplay}" />
                                                        <TextBlock Text="{x:Bind ConditionDisplay}" Opacity="0.75" />
                                                        <TextBlock Text="{x:Bind PurchaseDisplay}" Opacity="0.75" />
                                                        <TextBlock Text="{x:Bind PurchaseDateDisplay}" Opacity="0.75" />
                                                        <TextBlock Text="{x:Bind LocationDisplay}" Opacity="0.75" />
                                                        <TextBlock Text="{x:Bind Notes}" TextWrapping="Wrap" Opacity="0.75" />
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Top">
                                                        <Button Content="Edit" Tag="{x:Bind Id}" Click="EditCopyButton_Click" />
                                                        <Button Content="Delete" Tag="{x:Bind Id}" Click="DeleteCopyButton_Click" />
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </Border>

                        <Border Padding="20" CornerRadius="12"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <StackPanel Spacing="12">
                                <TextBlock Text="Overview" FontSize="20" FontWeight="SemiBold" />
                                <TextBlock x:Name="SummaryText" Text="No summary is available." TextWrapping="Wrap" LineHeight="23" />
                            </StackPanel>
                        </Border>

                        <Border Padding="20" CornerRadius="12"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <Grid RowSpacing="14" ColumnSpacing="24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Genre" FontWeight="SemiBold" />
                                <TextBlock x:Name="GenreText" Grid.Column="1" Text="Not recorded" TextWrapping="Wrap" />
                                <TextBlock Grid.Row="1" Text="Director" FontWeight="SemiBold" />
                                <TextBlock x:Name="DirectorText" Grid.Row="1" Grid.Column="1" Text="Not recorded" TextWrapping="Wrap" />
                                <TextBlock Grid.Row="2" Text="Studio" FontWeight="SemiBold" />
                                <TextBlock x:Name="StudioText" Grid.Row="2" Grid.Column="1" Text="Not recorded" TextWrapping="Wrap" />
                                <TextBlock Grid.Row="3" Text="Sort title" FontWeight="SemiBold" />
                                <TextBlock x:Name="SortTitleText" Grid.Row="3" Grid.Column="1" Text="Not recorded" TextWrapping="Wrap" />
                                <TextBlock Grid.Row="4" Text="Last Plex sync" FontWeight="SemiBold" />
                                <TextBlock x:Name="LastSyncedText" Grid.Row="4" Grid.Column="1" Text="Never" TextWrapping="Wrap" />
                                <TextBlock Grid.Row="5" Text="Artwork paths" FontWeight="SemiBold" />
                                <TextBlock x:Name="ArtworkText" Grid.Row="5" Grid.Column="1" Text="Not recorded" TextWrapping="Wrap" />
                            </Grid>
                        </Border>

                        <Border Padding="20" CornerRadius="12"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <StackPanel Spacing="12">
                                <TextBlock Text="External identifiers" FontSize="20" FontWeight="SemiBold" />
                                <TextBlock x:Name="PlexRatingKeyText" Text="Plex rating key: Not recorded" TextWrapping="Wrap" />
                                <TextBlock x:Name="PlexGuidText" Text="Plex GUID: Not recorded" TextWrapping="Wrap" />
                                <TextBlock x:Name="IMDbText" Text="IMDb ID: Not recorded" TextWrapping="Wrap" />
                                <TextBlock x:Name="TMDbText" Text="TMDb ID: Not recorded" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </ScrollViewer>

        <ProgressRing x:Name="LoadingRing" IsActive="True" Width="48" Height="48"
                      HorizontalAlignment="Center" VerticalAlignment="Center" />

        <ContentDialog x:Name="CopyDialog" PrimaryButtonText="Save" CloseButtonText="Cancel"
                       PrimaryButtonClick="CopyDialog_PrimaryButtonClick">
            <ScrollViewer MaxHeight="560">
                <StackPanel Spacing="12" MinWidth="460">
                    <TextBlock x:Name="CopyDialogHeading" Text="Add owned copy" FontSize="22" FontWeight="SemiBold" />
                    <ComboBox x:Name="FormatCombo" Header="Format" IsEditable="True" PlaceholderText="Choose or type a format">
                        <x:String>4K UHD</x:String>
                        <x:String>Blu-ray</x:String>
                        <x:String>DVD</x:String>
                        <x:String>Digital</x:String>
                        <x:String>VHS</x:String>
                        <x:String>LaserDisc</x:String>
                    </ComboBox>
                    <TextBox x:Name="EditionTextBox" Header="Edition" PlaceholderText="Standard, Collector's Edition, Criterion, etc." />
                    <ComboBox x:Name="PackagingCombo" Header="Packaging" IsEditable="True" PlaceholderText="Choose or type packaging">
                        <x:String>Standard Case</x:String>
                        <x:String>SteelBook</x:String>
                        <x:String>Digibook</x:String>
                        <x:String>Box Set</x:String>
                        <x:String>Slipcover</x:String>
                    </ComboBox>
                    <ComboBox x:Name="ConditionCombo" Header="Condition" IsEditable="True" PlaceholderText="Choose or type condition">
                        <x:String>New</x:String>
                        <x:String>Like New</x:String>
                        <x:String>Very Good</x:String>
                        <x:String>Good</x:String>
                        <x:String>Acceptable</x:String>
                    </ComboBox>
                    <TextBox x:Name="StoreTextBox" Header="Purchased from" PlaceholderText="Amazon, Walmart, Best Buy, etc." />
                    <NumberBox x:Name="PriceNumberBox" Header="Purchase price" Minimum="0" SpinButtonPlacementMode="Compact" />
                    <CalendarDatePicker x:Name="PurchaseDatePicker" Header="Purchase date" />
                    <TextBox x:Name="LocationTextBox" Header="Storage location" PlaceholderText="Living room - Shelf 2" />
                    <TextBox x:Name="NotesTextBox" Header="Notes" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="90" />
                    <CheckBox x:Name="DigitalCheckBox" Content="Digital copy" />
                    <CheckBox x:Name="FavoriteCheckBox" Content="Favorite or preferred copy" />
                    <InfoBar x:Name="CopyDialogInfoBar" IsOpen="False" Severity="Error" IsClosable="False" />
                </StackPanel>
            </ScrollViewer>
        </ContentDialog>
    </Grid>
</Page>
